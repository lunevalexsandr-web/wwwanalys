services:
  db:
    # pgvector/pgvector:pg15 = тот же PostgreSQL 15 + расширение vector.
    # Формат данных идентичен postgres:15, том сохраняется — совместимая замена.
    image: pgvector/pgvector:pg15
    environment:
      POSTGRES_DB: wwwanalys
      POSTGRES_USER: wwwanalys_user
      POSTGRES_PASSWORD: wwwanalys_password
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wwwanalys_user -d wwwanalys"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        PYTHON_VERSION: 3.11-slim
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://wwwanalys_user:wwwanalys_password@db:5432/wwwanalys
      SECRET_KEY: ${SECRET_KEY:-change-me-to-a-secret-key}
      API_KEY: ${API_KEY:-change-me-api-key}
      CORS_ORIGINS: http://localhost:5173,http://localhost:80
      AI_ENABLED: ${AI_ENABLED:-true}
      AI_PROVIDER: ${AI_PROVIDER:-auto}
      AI_MODEL: ${AI_MODEL:-claude-opus-4-8}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      OPENROUTER_BASE_URL: ${OPENROUTER_BASE_URL:-https://openrouter.ai/api/v1}
      OPENROUTER_MODEL: ${OPENROUTER_MODEL:-anthropic/claude-opus-4-8}
      RAG_EMBEDDING_PROVIDER: ${RAG_EMBEDDING_PROVIDER:-none}
      EMBEDDING_DIM: ${EMBEDDING_DIM:-1024}
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  postgres_data: